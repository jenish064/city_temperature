Aim: To make a request to the external server using the API.

Approach: There are 4 methods to do so using the external libraries,  but here we gonna do it with the native node library. Which is 'https'.
We have to require the 'https'.